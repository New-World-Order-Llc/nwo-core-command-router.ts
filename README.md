# nwo-core-command-router.ts
Routes validated command envelopes to deterministic subsystem handlers. Applies contract‑defined routing rules, ensures reproducible command flow, and emits immutable routing packets. Guarantees nondestructive, audit‑aligned command distribution across Beast System 3.0.
